{
  "name": "discord-oauth2-clientcredentials",
  "version": "1.0.0",
  "description": "A simple library to generate client credentials for Discord applications.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "discord",
    "client credentials",
    "bearer token"
  ],
  "author": "vaught-dawson",
  "license": "ISC",
  "dependencies": {
    "btoa": "^1.2.1",
    "node-fetch": "^2.6.1"
  }
}
